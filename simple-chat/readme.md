#### Simple chat
- `node server-7777.js` in a terminal on machine a
- `node server-8888.js` in a terminal on machine b, so long as b is on the same network as a
- `telnet localhost 8888` on machine a
- `telnet localhost 7777` on machine b
- to kill `telnet`, run `ctrl + ]` and type in `close`
